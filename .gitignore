# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
.env
.venv
env/
venv/
ENV/
*/ENV/
*/env/
*/venv/

# IDE / Editor specific
.vscode/
.idea/

# Test artifacts
.pytest_cache/
htmlcov/
.coverage

# Build artifacts
build/
dist/
*.egg-info/

# Local instance / secrets
instance/
# *.sqlite3 (if you use SQLite locally and don't want to commit the DB)
# .env (already covered above, but good to remember for secrets)
# Consider adding .env.example to your repository if you have local .env files with secrets

# Docker cache / data (if not desired in repo)
# .dockerignore is preferred for docker build context, but sometimes useful here too
# logs/
# *.log 